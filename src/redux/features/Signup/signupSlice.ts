import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const signuppSlice = createSlice({
  name: "signup",
  initialState,
  reducers: {},
});
export default signuppSlice.reducer;
